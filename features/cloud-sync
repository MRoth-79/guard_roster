/** Re-export cloud helpers (implementation lives in google-sheet.js for Pages compatibility). */
export {
  askCloudPassword,
  makeCloudSnapshot,
  applyCloudSnapshot,
  postToCloud,
  saveToCloud,
  loadFromCloud,
} from "./google-sheet.js";
